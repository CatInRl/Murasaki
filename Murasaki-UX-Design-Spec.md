# Murasaki UX 设计规范

> 基于现有 Murasaki UI 风格（紫色品牌色 + 暖灰中性色 + 8px 圆角体系）的完整 UX 设计参考。

## 1. 设计原则

- **写作优先**：所有 UI 元素不应干扰编辑区，次要操作收敛到菜单、面板、弹窗。
- **一致反馈**：相同交互在所有页面使用相同的视觉反馈（悬停、按下、聚焦、禁用、选中）。
- **最小打断**：优先使用 Toast、状态栏提示；弹窗仅用于需要用户决策或输入的场景。
- **明确状态**：任何动态区域必须覆盖空态、加载态、错误态、正常态、边界态。
- **键盘友好**：所有菜单和弹窗都标注快捷键，支持 Esc 关闭、Enter 确认、Tab 切换焦点。

## 2. 色彩与样式体系

参考 `colors_and_type.css`：

| 用途 | 变量 | 说明 |
|---|---|---|
| 品牌主色 | `--murasaki-primary` (#9333ea) | 主按钮、选中态、链接、进度 |
| 品牌前景 | `--murasaki-primary-foreground` (#fff) | 主按钮文字 |
| 背景 | `--murasaki-background` (#fff) | 页面背景 |
| 卡片 | `--murasaki-card` (#fafafa) | 面板、卡片 |
| 浮层 | `--murasaki-popover` (#fff) | 菜单、弹窗、Toast |
| 主文字 | `--murasaki-foreground` (#171717) | 标题、正文 |
| 次要文字 | `--murasaki-muted-foreground` (#737373) | 说明、快捷键 |
| 边框/分割线 | `--murasaki-border` (#e5e5e5) | 卡片边框、菜单分割线 |
| 成功 | `--murasaki-state-success` (#16a34a) | 保存成功、勾选 |
| 警告 | `--murasaki-state-warning` (#d97706) | 提示、可清理项 |
| 错误 | `--murasaki-state-error` (#dc2626) | 删除、错误、权限不足 |
| 信息 | `--murasaki-state-info` (#2563eb) | 信息提示 |

圆角：默认 8px（按钮/输入框/卡片），大卡片 16px，标签/头像 pill。
阴影：静态卡片 alpha ≤ 0.05；浮层（弹窗、菜单、Toast）使用 `shadow-2`。

## 3. 页面结构与导航

### 3.1 主窗口布局

```
┌─────────────────────────────────────────────────────────────┐
│  菜单栏（原生）                                               │
├──────────┬──────────────────────────────┬───────────────────┤
│          │                              │                   │
│  侧边栏   │          编辑区               │   Agent 面板      │
│ 文件树/  │      （源码 / 预览 / WYSIWYG） │   （可选）         │
│  大纲    │                              │                   │
│          │                              │                   │
├──────────┴──────────────────────────────┴───────────────────┤
│  状态栏                                                      │
└─────────────────────────────────────────────────────────────┘
```

- 侧边栏：文件树（Ctrl+Shift+E）与大纲（Ctrl+Shift+M）互斥切换。
- Agent 面板：固定右侧 380px，可通过设置开关显示。
- 底部面板：搜索结果显示区，可展开/收起。

### 3.2 标签页

- Tab 标题仅显示文件名，hover 显示完整绝对路径。
- 每个 tab 右侧有 × 关闭按钮；支持中键关闭、Ctrl+W、Ctrl+Tab 切换。
- 关闭最后一个 tab 返回欢迎页。
- 未保存时标题旁显示小圆点指示器。

## 4. 弹窗（Dialogs）

弹窗用于需要用户决策或输入的场景。统一规范：

- 居中显示，最大宽度 480px（大表单可 640px）。
- 背景遮罩 `rgba(0,0,0,.4)`，带轻微模糊。
- 圆角 16px，白色背景，顶部标题、中部内容、底部按钮。
- 按钮顺序（从左到右）：取消/次要操作 → 主操作。
- 主操作使用 `bg-primary`；破坏性操作使用 `bg-state-error`。
- 支持 Esc 关闭，Enter 触发主操作（除非是二次确认）。

### 4.1 弹窗清单

| 弹窗 | 触发条件 | 按钮/操作 | 备注 |
|---|---|---|---|
| 保存更改？ | 关闭未保存文件/退出 | 不保存 / 取消 / 保存 | 保存为主操作 |
| 移至回收站？ | 删除文件 | 取消 / 移至回收站 | 破坏性操作；可选“不再询问” |
| 目标已存在 | 拖拽/粘贴冲突 | 取消 / 重命名 / 覆盖 | 目录禁止覆盖 |
| 文件已在外部修改 | 应用聚焦/切 tab 检测到变更 | 忽略 / 重新加载 | 提示未保存冲突 |
| 新建文件 | 无工作区时新建 | 取消 / 创建 | 需选择保存路径 |
| 导出 HTML | 文件菜单 → 导出 HTML | 取消 / 导出 | 路径输入 + “嵌入图片为 Base64” |
| 确认大范围替换？ | Agent propose_replace 超过 50 行 | 取消 / 确认替换 | 显示影响行数 |
| 关于 Murasaki | 帮助菜单 | 关闭 | 版本、GitHub 链接 |

## 5. 菜单与上下文菜单

### 5.1 菜单栏

- 文件：新建文件 Ctrl+N、打开文件 Ctrl+O、打开文件夹 Ctrl+Shift+O、保存 Ctrl+S、另存为 Ctrl+Shift+S、导出 HTML、退出。
- 编辑：撤销 Ctrl+Z、重做 Ctrl+Y、剪切 Ctrl+X、复制 Ctrl+C、粘贴 Ctrl+V、全选 Ctrl+A、查找 Ctrl+F、在文件中查找 Ctrl+Shift+F、替换 Ctrl+H。
- 查看：切换文件树 Ctrl+Shift+E、切换大纲 Ctrl+Shift+M、显示/隐藏状态栏 Alt+Shift+S、全屏 F11。
- 帮助：设置 Ctrl+Comma、关于 Murasaki。

### 5.2 文件树右键菜单

打开、在新标签页打开、重命名 F2、剪切 Ctrl+X、复制 Ctrl+C、粘贴 Ctrl+V、删除 Del、在资源管理器中显示。

### 5.3 标签右键菜单

关闭、关闭其他、关闭右侧、复制路径、在资源管理器中显示。

### 5.4 编辑器右键菜单

撤销、重做、剪切、复制、粘贴、粘贴为纯文本、全选、查找、在文件中查找。

### 5.5 Agent 消息右键菜单

复制、插入到光标处、创建新文件、清空对话。

菜单样式：
- 背景 `--murasaki-popover`，圆角 8px，边框 `--murasaki-border`，阴影 `shadow-2`。
- 菜单项高度 32px，左侧图标 + 文字，右侧快捷键用 `muted-foreground`。
- 悬停背景 `--murasaki-muted`；禁用项 `opacity-50` 且不可点。
- 分割线为 1px `--murasaki-border`。

## 6. Toast 与通知

- 位置：窗口右上角，最多同时显示 3 个，自动堆叠。
- 宽度 320px，圆角 8px，背景 `--murasaki-popover`，阴影 `shadow-2`。
- 左侧 4px 状态色条：成功绿、警告橙、错误红、信息蓝、进度紫。
- 包含图标、消息、可选操作按钮、关闭按钮。
- 自动消失：成功/信息 4s；错误/警告常驻至用户关闭或点击操作。
- 进度 Toast 显示线性进度条。

### 6.1 Toast 清单

| 类型 | 图标 | 文案示例 | 操作 |
|---|---|---|---|
| 成功 | check-circle | 已保存 note.md | 无 |
| 信息 | info | 自动保存已开启 | 无 |
| 警告 | alert-triangle | 3 个孤立对话可清理 | 查看 |
| 错误 | x-circle | 无法读取文件：权限不足 | 重试 |
| 进度 | loader | 正在导出 HTML… | 无（带进度条） |
| 删除 | trash-2 | note.md 已移至回收站 | 撤销 |

状态栏提示：
- 加载中：左侧小 spinner + “正在保存…”
- 可清理：orphan chats 提示 “N 个孤立对话可清理 · 清理”

## 7. 空态与错误态

统一空态结构：中性图标（48px，muted）+ 一行标题 + 一行说明 + 可选次要按钮。

| 场景 | 图标 | 标题 | 说明 | 操作 |
|---|---|---|---|---|
| 未打开工作区 | folder-open | 未打开工作区 | 打开文件夹或文件开始编辑 | 打开文件夹 / 打开文件 |
| 空文件夹 | folder | 空文件夹 | 此文件夹中没有文件 | 新建文件 |
| 无大纲 | list | 无大纲 | 当前文档没有可识别的标题 | 无 |
| 搜索无结果 | search | 未找到结果 | 尝试其他关键词或正则表达式 | 无 |
| 无最近文件 | clock | 无最近文件 | 打开的文件会出现在这里 | 无 |
| 文件不存在 | file-x | 文件不存在 | 路径 /path/note.md 无法访问 | 关闭标签页 |
| 权限不足 | shield-alert | 权限不足 | 无法读取该文件，请检查权限 | 重试 |
| AI 服务不可用 | wifi-off | AI 服务不可用 | 无法连接到 DeepSeek API，请检查网络或 API Key | 检查设置 |
| 网络错误 | wifi-off | 网络错误 | 请求超时，请稍后重试 | 重试 |

加载态：使用骨架屏，结构与最终内容一致，避免 spinner 单独占据大面积。

## 8. 浮层与工具栏

### 8.1 常用浮层

| 浮层 | 内容 |
|---|---|
| 插入链接 | 链接地址输入框 + “在新标签页打开”复选框 + 取消/确认 |
| 插入图片 | 图片路径输入框 + 提示“支持相对路径或拖拽图片” + 取消/插入 |
| 插入表格 | 行数/列数输入 + 取消/插入 |
| 标题选择器 | H1/H2/H3/H4/正文/引用，当前项高亮 |
| 命令面板 | 搜索输入 + 最近打开/命令/文件分类列表 |
| 表情选择器 | 最近使用 + 分类网格 |
| 主题选择 | 浅色 / 深色 / 跟随系统 单选 |

### 8.2 WYSIWYG 工具栏

- 背景 `--murasaki-surface`，底部边框 `--murasaki-border`，圆角顶部 8px。
- 按钮分组：加粗、斜体、删除线、行内代码；链接、标题、引用；无序列表、有序列表、任务列表；插入图片。
- 当前激活态使用 `bg-primary/10 text-primary`。
- 悬停背景 `--murasaki-muted`。

## 9. 面板

### 9.1 搜索结果面板

- 位于窗口底部，可拖动高度。
- 顶部：搜索输入框、正则开关、仅文件名开关、结果计数、关闭按钮。
- 结果列表：文件路径 + 行号 + 匹配片段高亮。
- 点击结果在编辑器中定位并打开文件。

### 9.2 大纲面板

- 位于左侧栏，显示文档标题层级树。
- 当前所在标题高亮 `bg-primary/10 text-primary`。
- 点击滚动到对应标题。

### 9.3 文件树面板

- 左侧栏默认显示。
- 文件夹可展开/折叠，文件显示图标，当前打开文件高亮 `bg-primary text-primary-foreground`。
- 拖拽文件到文件夹实现物理移动；外部文件拖入则复制到目标目录。
- 命名冲突统一弹出“覆盖/重命名/取消”对话框。

### 9.4 Agent 面板

- 固定右侧 380px。
- 顶部：标题“AI 助手”、当前 provider 标签、清空对话按钮。
- 中部：对话流，用户消息在右，助手消息在左；工具调用折叠显示。
- 输入框：底部固定，发送/停止按钮，上下文卡片显示当前文档与 token 估算。
- 提案：内联装饰（绿色插入/红色删除）+ 右侧浮动 ✓/✗。

### 9.5 状态栏

- 左侧：当前文件绝对路径（截断显示）、保存状态。
- 中部：光标行:列、字符数（不含空格）。
- 右侧：orphan chats 提示、AI provider、编辑器模式（源码/WYSIWYG）。
- 全屏 F11 时自动隐藏；可通过 Alt+Shift+S 手动切换。

## 10. 交互状态规范

所有可交互元素必须覆盖以下状态：

| 元素 | 默认 | 悬停 | 按下 | 聚焦 | 禁用 | 选中 |
|---|---|---|---|---|---|---|
| 主按钮 | bg-primary text-white | 亮度+5% | scale(0.99) | ring-2 ring-ring | opacity-50 | - |
| 次要按钮 | bg-transparent border | bg-muted | - | ring-2 | opacity-50 | - |
| 输入框 | border-input bg-background | border-muted-foreground | - | ring-2 ring-ring border-primary | bg-muted | - |
| 链接 | text-primary | underline | - | outline-2 | opacity-50 | - |
| 列表行 | bg-transparent | bg-muted | - | - | - | bg-primary text-white |
| 菜单项 | bg-transparent | bg-muted | - | - | opacity-50 | bg-primary/10 text-primary |

## 11. 拖放反馈

- 拖拽文件经过可放置区域：边框变为虚线 `border-dashed border-primary`，背景 `bg-primary/5`。
- 放置成功：Toast 提示；失败：错误 Toast。
- 拖拽图片到编辑器：插入相对路径，图片自动复制到 assets/。

## 12. 快捷键总览

| 操作 | 快捷键 |
|---|---|
| 新建文件 | Ctrl+N |
| 打开文件 | Ctrl+O |
| 打开文件夹 | Ctrl+Shift+O |
| 保存 | Ctrl+S |
| 另存为 | Ctrl+Shift+S |
| 关闭标签 | Ctrl+W |
| 切换标签 | Ctrl+Tab / Ctrl+Shift+Tab |
| 撤销 | Ctrl+Z |
| 重做 | Ctrl+Y |
| 查找 | Ctrl+F |
| 在文件中查找 | Ctrl+Shift+F |
| 切换文件树 | Ctrl+Shift+E |
| 切换大纲 | Ctrl+Shift+M |
| 显示/隐藏状态栏 | Alt+Shift+S |
| 全屏 | F11 |
| 重新加载文件 | Ctrl+R |
| 设置 | Ctrl+Comma |

## 13. 文案规范

- 标题：名词短语，最多 8 个汉字。
- 按钮：2–6 个汉字，如“保存”、“取消”、“打开文件夹”。
- 说明：简洁直接，避免技术术语，必要时给出可操作建议。
- 错误信息：说明“发生了什么” + “用户能做什么”。
- 所有用户可见文本使用中文，仅品牌名与通用缩写保留英文。
