## 变更摘要

<!-- 一句话说清这个 PR 做了什么、为什么 -->

## 关联 issue

<!-- 二选一（删掉不适用的那个）：
- Closes #N   —— 该 issue 的活在本 PR 完成
- Part of #N  —— spec issue 的子任务之一
-->

Closes #

## 验证方式

<!-- 列出实际跑过的命令与手工验证项。CI 会跑 npm test / npm run build（ubuntu）与 npm run build / cargo test（windows） -->

- [ ] `npm test`
- [ ] `npm run build`
- [ ] `npm run test:rust`
- [ ] 手工验证：

## 风险与回滚

<!-- 影响面与可能的回归点；回滚方式（通常为 revert 本 PR 的 squash 提交） -->